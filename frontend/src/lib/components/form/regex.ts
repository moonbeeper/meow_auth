export const PIN_DIGIT_REGEX = /^\d+$/;
export const PIN_DIGIT_AND_CHAR = /^[a-zA-Z0-9]+$/;
// it practically tells you if there's ONE space. So you better to it on all chars lol
export const SPACE = /\s/;
