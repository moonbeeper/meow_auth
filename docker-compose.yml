services:
  database:
    image: "postgres:18-alpine"
    volumes:
      - meow-db-data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: meow
      POSTGRES_PASSWORD: meow_pwd
      POSTGRES_DB: meow2_development

volumes:
  meow-db-data:
